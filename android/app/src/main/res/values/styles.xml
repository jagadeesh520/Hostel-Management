<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

  <!-- Edge-to-edge base theme that libraries expect -->
  <style name="Theme.EdgeToEdge" parent="Theme.MaterialComponents.DayNight.NoActionBar">
    <!-- Allow drawing behind system bars -->
    <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    <item name="android:statusBarColor">@android:color/transparent</item>
    <item name="android:navigationBarColor">@android:color/transparent</item>

    <!-- Material colors (override as needed) -->
    <item name="colorPrimary">#5e72e4</item>
    <item name="colorPrimaryVariant">#4b61d8</item>
    <item name="colorOnPrimary">#ffffff</item>

    <!-- Light/dark bar icons (set to true if using light background) -->
    <item name="android:windowLightStatusBar">false</item>
    <item name="android:windowLightNavigationBar">false</item>
  </style>

  <!-- Your app theme inheriting the EdgeToEdge theme -->
  <style name="AppTheme" parent="Theme.EdgeToEdge">
    <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="android:statusBarColor">#ffffff</item>
  </style>

  <!-- Splash theme (keep these if you use Android 12 splash API) -->
  <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
    <item name="windowSplashScreenBackground">@color/splashscreen_background</item>
    <item name="windowSplashScreenAnimatedIcon">@drawable/splashscreen_logo</item>
    <item name="postSplashScreenTheme">@style/AppTheme</item>
  </style>

</resources>
